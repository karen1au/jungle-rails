Thanks you for your order <%= @order.email %>
==============================================
Your order is being processed. Here are the details for order# <%= @order.id %>:

<% @order.line_items.each do |item| %>
<%= render 'text', product: Product.find(item[:product_id]), quantity: item[:quantity].to_i %>
<% end %>

TOTAL:<%= @order.total_cents / 100.0 %>

Exchange & Returns:
You can return an item to any store, or through Canada Post—free of charge—within 30 days of the date your order is fulfilled.